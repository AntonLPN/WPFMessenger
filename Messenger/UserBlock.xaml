<UserControl x:Class="Messenger.UserBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Messenger"
             mc:Ignorable="d" Height="65" Width="258">
    <Grid Background="#FFA2A2A2" Width="258" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="19*" ></ColumnDefinition>
            <ColumnDefinition Width="46*" ></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Border Width="60" Height="60">
            <Image Grid.Column="0" x:Name="UserImage" Width="60" Height="60"></Image>
        </Border>
       
        <TextBlock x:Name="UserName" Grid.Column="1" HorizontalAlignment="Left" Margin="5,4,0,5" TextWrapping="Wrap" Text="User name" VerticalAlignment="Top" Width="220" FontSize="16" FontWeight="Bold" FontFamily="Times New Roman"/>

    </Grid>
</UserControl>
